import 'package:flutter/material.dart';

class errorPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      body: Text("404",style: TextStyle(backgroundColor: Colors.blue),),
    );
  }
}